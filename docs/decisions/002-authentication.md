# Decision 002 — Shared Firebase authentication

**Status:** Accepted

The Console and Portal share one Firebase project. Console access requires both a valid authenticated account and an `admins/{uid}` authorisation record.
